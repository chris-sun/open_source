#!/usr/bin/python

class LinkedList

	value = 0
	prev = None
	next = None
	head = None
	last = None

	def __init__(self, value):
		self.value = value
		self.head = self
		self.last = self

	def append(value)
		next = LinkedList( 
